@model COMP2084_MusicStore.Models.ShoppingCartViewModel

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            @*<th>
                    @Html.DisplayNameFor(model => model.ShoppingCartId)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Count)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Song)
                </th>
                <th></th>*@
            <th>Song</th>
            <th>Album</th>
            <th>Price</th>
            <th>Quantity</th>
            <th>Remove</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.CartItems)
        {
            <tr>
                <td>
                    @Html.DisplayFor(x => item.Song.Title)
                </td>
                @*td = table data, th = table header*@
                <td>
                    @Html.DisplayFor(x => item.Song.Album.Title)
                </td>
                <td>
                    @Html.DisplayFor(x => item.Song.Price)
                </td>
                <td>
                    @Html.DisplayFor(x => item.Count)
                </td>
                <td>
                    @Html.ActionLink("Remove from cart", "RemoveFromCart", "ShoppingCart", new { SongId = item.SongId })
                </td>
                @*<td>
                        @Html.DisplayFor(modelItem => item.ShoppingCartId)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Count)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Song.SongId)
                    </td>
                    <td>
                        <a asp-action="Edit" asp-route-id="@item.ShoppingCartLineItemId">Edit</a> |
                        <a asp-action="Details" asp-route-id="@item.ShoppingCartLineItemId">Details</a> |
                        <a asp-action="Delete" asp-route-id="@item.ShoppingCartLineItemId">Delete</a>
                    </td>*@

                /**/
            </tr>
        }
    </tbody>
</table>
Subtotal: <strong>@Model.CartTotal.ToString("C")</strong>
@*"C" currency*@